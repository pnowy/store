<?xml version="1.0" encoding="utf-8"?>
<databaseChangeLog
        xmlns="http://www.liquibase.org/xml/ns/dbchangelog"
        xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
        xsi:schemaLocation="http://www.liquibase.org/xml/ns/dbchangelog http://www.liquibase.org/xml/ns/dbchangelog/dbchangelog-3.5.xsd">

    <property name="now" value="now()" dbms="h2"/>
    <property name="now" value="current_timestamp" dbms="postgresql"/>

    <!-- HIBERNATE ENVERS REVISION INFO TABLE -->
    <changeSet id="0000" author="pnowy">
        <createTable tableName="revinfo">
            <column name="rev" type="bigint" autoIncrement="true">
                <constraints primaryKey="true" nullable="false"/>
            </column>
            <column name="revtstmp" type="bigint"/>
        </createTable>
    </changeSet>

    <!-- PRODUCT TABLES -->
    <changeSet id="0001" author="pnowy">
        <!-- CURRENT ENTITY STATE -->
        <createTable tableName="st_product">
            <column name="id" type="bigint" autoIncrement="true">
                <constraints primaryKey="true" nullable="false"/>
            </column>
            <column name="name" type="varchar(255)">
                <constraints nullable="false" />
            </column>
            <column name="price" type="DECIMAL">
                <constraints nullable="false" />
            </column>
        </createTable>

        <!-- PRODUCT REVISIONS by HIBERNATE ENVERS -->
        <createTable tableName="st_product_audit">
            <column name="id" type="bigint">
                <constraints nullable="false"/>
            </column>
            <column name="rev" type="bigint">
                <constraints nullable="false"/>
            </column>
            <column name="revtype" type="smallint">
                <constraints nullable="false" />
            </column>
            <column name="name" type="varchar(255)">
                <constraints nullable="false"/>
            </column>
            <column name="price" type="decimal">
                <constraints nullable="false"/>
            </column>
        </createTable>

        <addPrimaryKey tableName="st_product_audit" columnNames="id,rev" />
        <addForeignKeyConstraint constraintName="fk_product_audit_revinfo"
                                 baseTableName="st_product_audit" baseColumnNames="rev"
                                 referencedTableName="revinfo" referencedColumnNames="rev" />

    </changeSet>



</databaseChangeLog>
